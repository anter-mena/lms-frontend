# Copy this file to `.env.local` and fill in the real values.

# Address of the backend API, reachable from the machine running Next.js.
# Server-side only — never prefix with NEXT_PUBLIC_.
BACKEND_URL=http://<host>:<port>
